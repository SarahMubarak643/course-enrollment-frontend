// Matches courseEnrollement.example.demo.entity.Student
export interface Student {
  studentId: number;
  studentNumber: string;
  fullName: string;
  email: string;
  active: boolean;
  createdAt: string;
}
